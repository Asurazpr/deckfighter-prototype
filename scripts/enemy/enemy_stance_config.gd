class_name EnemyStanceConfig
extends RefCounted

# TODO: Move stance tuning into per-character kit data when enemies get
# separate normal/elite/boss profiles, armor rules, and boss phase overrides.

const MAX_STANCE := 100
const BREAK_HITSTUN_FRAMES := 45
const BREAK_BLOCK_RECOVERY_FRAMES := 30
const PROTECTION_RECOVERY_FRAMES := 30
